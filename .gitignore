# Firebase local
.firebase/
firebase-debug.log*
firestore-debug.log*
ui-debug.log*

# Cloud Functions dependencies
functions/node_modules/

# Flutter and Dart generated files
.dart_tool/
.flutter-plugins
.flutter-plugins-dependencies
.packages
build/
coverage/

# Android release signing — never commit private keys or passwords
android/key.properties
android/app/*.jks
android/app/*.keystore

# Ancienne copie imbriquée locale
/geopoint/
